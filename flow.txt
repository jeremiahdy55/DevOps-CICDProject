1. Have the microservice(s) code in the Github monorepo
2. Create the terraform scripts.
3. Create an EC2 instance for terraform to run on.
4. Configure AWS credentials using command: "aws configure" in the terraform EC2 instance
4a. *Need to use IAM roles to generate access key and secret access key*
5. Upload the .tf scripts into the terraform EC2 instance, then run:
5a. "terraform init"
5b. "terraform plan"
5c. "terraform apply"
6. Log into Jenkins
6a. sudo cat /var/lib/jenkins/secrets/initialAdminPassword
6b. Install suggested plugins for Jenkins 
7. Configure Github and Docker credentials for Jenkins
7a. Manage Jenkins > Credneitals > global (add credentials from dropdown)
7b. DOCKER - Username: Insert docker hub Username
7c. DOCKER - password: Insert docker hub personal access token
7d. DOCKER - ID: variable name for Jenkins to access this information in scripts and jobs

7e. GITHUB - Username: Insert Github hub Username
7f. GITHUB - password: Insert Github hub personal access token
7g. GITHUB - ID: variable name for Jenkins to access this information in scripts and jobs

8. Configure Jenkins Pipeline
8a. Dashboard > new Item > Pipeline
8b. Select any triggers you want (GitHub hook trigger for this project)
8c. In PIPELINE section, select (Pipeline script from SCM)
8d. SCM : "Git"
8e. Repository URL: github repo URL
8f. Credentials: (Select from the dropdown menu the Github credentials set up in step 7)
8g. Specify which branch to use (usually */main)
8h. Specify Jenkinsfile path (usually keep it in root directory, so just "Jenkinsfile")

9. Configure Github's side of the Github Webhook for Jenkins
9a. Go to project repo
9b. Settings > Webhooks > Add Webhook
9c. Navigate Jenkins (Manage Jenkins > System > Github Section, click on ? next to Github Servers)
9d. Copy webhook URL from Jenkins and paste in Github Webhook Payload URL
9e. Set content type to application/json
9f. Select "just the push event"
9g. Save webhook





terraform setup
sudo apt update
sudo apt upgrade
sudo apt install -y gnupg software-properties-common curl
curl -fsSL https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
sudo apt update
sudo apt install -y terraform

********************************************************************************************************************************
use IAM access key via AWS CLI
sudo su
   sudo apt install unzip
   curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
   unzip awscliv2.zip
   sudo ./aws/install
   aws --version

////// AWS CONFIGURE (USE ACCESS KEY) ///////
aws configure
insert access key
insert secret key
use region available
leave default output blank


